---
title: "CPU"
sidebar_position: 6
sidebar_label: "CPU"
slug: /computer-fundamentals/cpu
description: "CPU - Computer Fundamentals | Open CS Hub"
---

import RunInLab from '@site/src/components/RunInLab';
import AskBox from '@site/src/components/AskBox';

# CPU

## Overview

The **CPU (Central Processing Unit)** is often called the **"brain"** of the computer. Every task the computer performs — calculations, decisions, running programs — is controlled and processed by the CPU.

For example, when we run a program — say a calculator app adding 5 + 3 — it is the CPU that actually performs the addition and tells the screen what to display. In short, every instruction in a computer passes through and is executed by the CPU.

Physically, the CPU is a small chip mounted on the motherboard. Even though it's small in size, it contains billions of transistors working together to perform all the calculations and logic operations.

## Key Concepts

- **ALU (Arithmetic Logic Unit)** — The part of the CPU that performs mathematical operations (add, subtract, multiply, divide) and logical operations (AND, OR, NOT).
- **CU (Control Unit)** — The part of the CPU that decides which instruction should run when, and coordinates the other parts of the computer (memory, input/output devices) with proper instructions.
- **Registers** — Very small but extremely fast memory locations inside the CPU, used to temporarily hold data the CPU is currently working with.
- **Cache Memory** — A small, high-speed memory located close to the CPU, storing frequently used data so the CPU doesn't have to fetch it from the slower RAM every time.
- **Clock Speed (GHz)** — Measures how many cycles (steps) the CPU can perform per second. For example, 3 GHz means 3 billion cycles per second.
- **Core** — A single CPU can have multiple "cores," where each core can work on a different task at the same time (Multi-core processor).
- **Fetch–Decode–Execute Cycle** — The basic cycle that describes how the CPU works: Fetch the next instruction, Decode what it means, then Execute it.

## How the CPU Works (Diagram)

The diagram below shows the **Fetch → Decode → Execute** cycle, along with the ALU and Control Unit that drive it.

<div style={{
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  flexWrap: 'wrap',
  gap: '1.5rem',
  padding: '2.5rem 1rem'
}}>
  <div style={{
    padding: '1rem 1.5rem',
    borderRadius: '12px',
    border: '2px solid #4f8cff',
    background: 'rgba(79,140,255,0.08)',
    fontWeight: 600,
    textAlign: 'center'
  }}>
    🧠 Fetch
  </div>

  <div style={{
    padding: '1rem 1.5rem',
    borderRadius: '12px',
    border: '2px solid #ff9f43',
    background: 'rgba(255,159,67,0.08)',
    fontWeight: 600,
    textAlign: 'center'
  }}>
    🔍 Decode
  </div>

  <div style={{
    padding: '1.4rem 2rem',
    borderRadius: '14px',
    border: '3px solid #2ecc71',
    background: 'rgba(46,204,113,0.1)',
    fontWeight: 700,
    textAlign: 'center'
  }}>
    ⚡ Execute
  </div>

  <div style={{
    padding: '1rem 1.5rem',
    borderRadius: '12px',
    border: '2px solid #a55eea',
    background: 'rgba(165,94,234,0.08)',
    fontWeight: 600,
    textAlign: 'center'
  }}>
    🧮 ALU
  </div>

  <div style={{
    padding: '1rem 1.5rem',
    borderRadius: '12px',
    border: '2px solid #ff6b6b',
    background: 'rgba(255,107,107,0.08)',
    fontWeight: 600,
    textAlign: 'center'
  }}>
    🎛️ Control Unit
  </div>
</div>

<div style={{
  textAlign: 'center',
  color: '#888',
  fontSize: '0.9rem',
  marginTop: '-1rem',
  marginBottom: '2rem'
}}>
  Fetch → Decode → Execute repeats billions of times per second, with the ALU and Control Unit working together throughout
</div>

> This repeating cycle is exactly what "Clock Speed (GHz)" measures — how many times per second the CPU can go through Fetch → Decode → Execute.

## Example

Let's take a simple Python program that adds two numbers:

```python
a = 5
b = 3
result = a + b
print(result)  # Output: 8

## Run your code

<RunInLab topic="cpu" lang="python" />

<AskBox topic="cpu" section="Computer Fundamentals" />
